  <div class="header home">
    <h1>WELCOME ON <br> GREEN MAP</h1>
  </div>
  <div class="introduction-cards-box">
    <div class="hero-box">
      <h4 class="hero">WANNA BE A VIGILANTEE ?</h4>
      <p>Be a Nature Vigilante ! Spot and ALLERT polluted sites to initiate a rescue mission! </p>
    </div>
    <div class="hero-box">
      <h4 class="hero">WANNA BE A SUPERHERO ?</h4>
      <p>Be a Nature Avengers ! Bring energy & tools to COLLECT, and go on a mission to rescue our landscape from pollution</p>
    </div>
  </div>

  </div>

  <div
    id="map"
    style="width: 100%;
    height: 500px;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  ></div>

  <%= render 'shared/home_navbar' %>
</div>

<script>
  let vh = window.innerHeight * 0.01;
  // Then we set the value in the --vh custom property to the root of the document
  document.documentElement.style.setProperty('--vh', `${vh}px`);
</script>
